# Data

Sample readings and test logs from LongLink.

## Planned contents

- `sample_readings.csv` - example sensor readings collected at the gateway
- Range test logs - distance, signal strength, and packet loss

## Columns

timestamp, node_id, sequence, [sensor columns], battery, signal_strength
